# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

variables:
  azureSubscription: MSDN_nicolasyuen

resources:
  containers:
  - container: rover
    image: aztfmod/rover:2001.0622
    options: --user 0:0 -v /tmp:/tmp -e TF_PLUGIN_CACHE_DIR="/tmp/tf-plugin-cache" -e TF_DATA_DIR="/tmp" #temporary workaround as the user needs USERADD rights

pool:
  vmImage: 'ubuntu-latest'

container: rover

steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: ${{variables.azureSubscription}}
    scriptType: bash
    scriptLocation: inlineScript
    inlineScript: |
        echo "Deploying the launchpad level 0 using Rover"
        echo "See https://github.com/aztfmod/rover"
        ls /tf
        /tf/rover/launchpad.sh
  displayName: 'Initialize the Level0 launchpad'

- task: AzureCLI@2
  inputs:
    azureSubscription: ${{variables.azureSubscription}}
    scriptType: bash
    scriptLocation: inlineScript
    inlineScript: |
        id=$(az resource list --tag stgtfstate=level0 | jq -r .[0].id)
        if [ -z "$id" ]
        then
           echo "Remote state not properly Initialized"
           exit 1
        else
           echo "Remote state Initialized with storage account $id"
        fi
  displayName: 'Verifying launchpad results'
